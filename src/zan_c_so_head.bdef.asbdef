projection;
strict ( 2 );

define behavior for zan_c_so_head //alias <alias_name>
use etag
{
  use create;
  use update;
  use delete;

  use association _item { create; }
}

define behavior for zan_c_so_item //alias <alias_name>
{
  use update;
  use delete;

  use association _head;
}
projection;
strict ( 2 );

define behavior for ZAAN_Connection_p //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use association _flight { create; }
}

define behavior for zaan_flight_p //alias <alias_name>
{
  use update;
  use delete;

  use association _connection;
}